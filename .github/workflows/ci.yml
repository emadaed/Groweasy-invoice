name: 🧪 CI - GrowEasy Invoice

on:
  push:
    branches: [ main, feature/* ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    name: ✅ Run Smoke & Unit Tests
    runs-on: ubuntu-latest
    env:
      APP_ENV: ${{ secrets.APP_ENV }}
      SECRET_KEY: ${{ secrets.SECRET_KEY }}
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      JWT_SECRET_KEY: ${{ secrets.JWT_SECRET_KEY }}
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      CLOUD_API_KEY: ${{ secrets.CLOUD_API_KEY }}
      EMAIL_SMTP_PASS: ${{ secrets.EMAIL_SMTP_PASS }}

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install flake8 black pytest pytest-cov

      - name: 🧹 Lint with flake8
        run: |
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
          flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

      - name: 🎨 Format check with Black
        run: black --check .

      - name: 🧪 Run Tests with Coverage
        run: |
          pytest --maxfail=1 --disable-warnings -q --cov=. --cov-report=term-missing

      - name: 📊 Upload Coverage Report (Artifact)
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: htmlcov/

  deploy:
    if: github.ref == 'refs/heads/main' && success()
    name: 🚀 Deploy to Cloud (Phase 3 Placeholder)
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: ⏳ Placeholder
        run: echo "CD pipeline will be activated in Phase 3."